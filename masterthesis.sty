\usepackage[ngerman,english]{babel} 

\usepackage[utf8]{inputenc} %input and fontencoding
\usepackage[T1]{fontenc}

\usepackage{amsmath,amsthm} %math environments; no errors with TUDesign
%\usepackage{amsmath,amsthm,amssymb} %math environments

\usepackage{textcomp} %registered trademark symbol \textregistered

\usepackage{cite} %use bibtex
\bibliographystyle{acm}
% literaturverwaltung
%\usepackage[backend=biber,
%            hyperref=true,
%            style=numeric,
%            maxcitenames=3,
%            maxbibnames=100,
%            block=none,
%            backref=true]{biblatex}
%\addbibresource{./bib/masterthesis.articles.bib}
%\addbibresource{./bib/masterthesis.books.bib}

\usepackage[intoc,noprefix]{nomencl} %for printing a list of symbols

%hyperlinks and urls
\usepackage{hyperref} %use hyperlinks in document
\usepackage{url} %display urls as such

\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usetikzlibrary{calc,arrows.meta,decorations.markings,arrows}
\usepackage{tkz-euclide} % loads  TikZ and tkz-base
\usetkzobj{angles} % important you want to use angles
%\usepackage[percent]{overpic} %superimpose pictures

\usepackage{subfigure}
%\usepackage{subcaption}
\usepackage{graphicx}
%\usepackage{sidecap} % sometimes its desirable to have a caption appear on the side of a float, rather than above or below

\usepackage{algorithm} %algorithms
\usepackage{algpseudocode}
\usepackage{algpascal}

\usepackage{setspace} %to use \doublespacing in tables
\usepackage{multirow} %multiple rows in tabular environments
\usepackage{booktabs} %provides extra tables commands and in general makes tables look a little nicer than without
\usepackage{colortbl} 
\usepackage{xcolor}   %are required to allow the colour of table and text elements to be altered
\usepackage{xfrac}    %is required to make nicer fractions that look good in tables


%\usepackage{subdepth} %vertical alignment of subscripts

\usepackage{listings} %load source code printer % use with \lstset{language=[77]Fortran} \begin{lstlisting} SOURCE \end{lstlisting}

%\usepackage{todonotes} %self explaining but not compatible with TUDesign

%selective compilation of files
%\includeonly{./tex/masterthesis.fvm,./tex/masterthesis.seg}

%custom commands commands
\newcommand{\ra}[1]{\renewcommand{\arraystretch}{#1}}
\renewcommand{\vec}[1]{\mathbf{#1}}
\newcommand{\vecg}[1]{\boldsymbol{#1}}
\providecommand{\e}[1]{\ensuremath{\times 10^{#1}}}
\algnewcommand\algorithmicto{\textbf{to}}
\algrenewtext{For}[2]%
{\algorithmicfor\ #1 \algorithmicto\ #2 \algorithmicdo}

%proof environment
\everymath=\expandafter{\the\everymath\displaystyle}
\theoremstyle{plain}
\newtheorem*{prop}{Proposition}

\makenomenclature
